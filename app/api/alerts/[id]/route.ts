import {NextResponse} from "next/server";import {createClient} from "@/lib/supabase/server";import {deleteAlert} from "@/lib/alerts/server";
export async function DELETE(_r:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const s=await createClient();const {data,error}=await s.auth.getUser();if(error||!data.user)return NextResponse.json({error:"Não autenticado."},{status:401});try{const ok=await deleteAlert(s,data.user,id);return ok?NextResponse.json({ok:true}):NextResponse.json({error:"Alerta não encontrado."},{status:404})}catch{return NextResponse.json({error:"Não foi possível excluir o alerta."},{status:503})}}
