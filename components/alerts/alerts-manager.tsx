"use client";
import {FormEvent,useState} from "react";import type {FlightAlert} from "@/types/alert";
export function AlertsManager({initialAlerts}:{initialAlerts:FlightAlert[]}){const [alerts,setAlerts]=useState(initialAlerts),[busy,setBusy]=useState(false),[error,setError]=useState("");
async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const f=new FormData(e.currentTarget);const payload=Object.fromEntries(f.entries());try{const r=await fetch("/api/alerts",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const b=await r.json().catch(()=>null);if(r.status===401){location.href="/login?next=/alerts";return}if(!r.ok)throw new Error(b?.error||"Não foi possível salvar.");setAlerts(a=>[b.alert,...a]);e.currentTarget.reset()}catch(err){setError(err instanceof Error?err.message:"Não foi possível salvar.")}finally{setBusy(false)}}
async function remove(id:string){setError("");const r=await fetch(`/api/alerts/${id}`,{method:"DELETE"});if(r.ok)setAlerts(a=>a.filter(x=>x.id!==id));else setError("Não foi possível excluir o alerta.")}
return <div className="alerts-layout"><form className="module-card alert-form" onSubmit={submit}><span className="module-status">Salvar rota</span><h2>Novo alerta</h2><div className="alert-form-grid"><label><span>Origem</span><input name="origin" placeholder="SAO" maxLength={40} required/></label><label><span>Destino</span><input name="destination" placeholder="LIS" maxLength={40} required/></label><label><span>Ida</span><input name="departure_date" type="date"/></label><label><span>Volta</span><input name="return_date" type="date"/></label><label><span>Moeda</span><select name="currency" defaultValue="USD"><option>USD</option><option>EUR</option><option>BRL</option><option>ARS</option><option>GBP</option></select></label><label><span>Preço-alvo opcional</span><input name="target_price" type="number" min="1" step="1" placeholder="750"/></label></div><button className="primary-trip-action" disabled={busy}>{busy?"Salvando…":"Salvar alerta"}</button><p className="alert-disclaimer">O alerta fica salvo na sua conta. A verificação automática de tarifas será ativada somente após a integração de um provedor real de voos.</p>{error?<p className="trips-error" role="alert">{error}</p>:null}</form><section className="saved-alerts"><div className="saved-alerts-heading"><h2>Seus alertas</h2><span>{alerts.length}</span></div>{alerts.length?alerts.map(a=><article key={a.id} className="saved-alert"><div><strong>{a.origin} → {a.destination}</strong><p>{a.departure_date||"Data flexível"}{a.return_date?` — ${a.return_date}`:""}</p>{a.target_price?<small>Meta: {a.currency} {a.target_price.toLocaleString("pt-BR")}</small>:<small>Sem preço-alvo</small>}</div><div className="saved-alert-actions"><span>Aguardando integração</span><button type="button" onClick={()=>remove(a.id)}>Excluir</button></div></article>):<div className="empty-inline"><h3>Nenhum alerta salvo</h3><p>Adicione uma rota para deixar sua preferência pronta para a integração de preços.</p></div>}</section></div>}
